using UnityEngine;

namespace PopupSystem.Popups.Data
{
    public class InfoPopupData : APopupData
    {
        public InfoPopupData(Priority priority, string message, string title) : base(priority, message, title)
        {
            
        }
    }
}